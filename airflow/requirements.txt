# Cosmos renders each dbt model as its own Airflow task; the [dbt-bigquery] extra
# installs a compatible dbt-bigquery into the Astro image (LOCAL execution mode).
astronomer-cosmos[dbt-bigquery]==1.8.2

# Runtime deps for the upstream `ingest` task (scripts/ingest.py).
kaggle==1.6.17
google-cloud-storage==2.19.0
